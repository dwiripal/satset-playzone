{
  "name": "satset-playzone",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html styles.css app.js manifest.webmanifest sw.js _redirects icons dist/"
  },
  "devDependencies": {}
}
